use crate::{cli::parser::Args, config::prelude::Registry};
use inquire::Text;

pub fn remote_add(
    args: &Args,
    registry: &Registry,
    name: &Option<String>,
    provider: &Option<String>,
) {
    todo!()
}
